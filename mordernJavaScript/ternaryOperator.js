    // One Line Ternary Operator.

let age = 21;

let type = (age >= 18) ? "adult" : "child";

console.log(type);

    // nested ternary operator.

let age1 = 10;
let type1 = (age1 >= 18) ? "adult" : (age1<10) ? "Child" : "Not a child and not a adult";

console.log(type1);